# Frontend build mode for the LOCAL appliance (thickness-local .deb).
# Empty VITE_SERVER_URL -> the app talks to window.location.origin, i.e. the
# same box that serves it. Built via: npx vite build --mode localapp
VITE_SERVER_URL=
