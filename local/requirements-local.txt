# Runtime deps compiled into the thickness-local binary.
# NO psycopg2 — the local build uses SQLite via backend/local_db.py.
Flask
Flask-SocketIO
simple-websocket
Werkzeug
itsdangerous
requests
cryptography
# Windows desktop launcher (backend/local_gui.py): tray icon + generated icon image.
pystray
Pillow
